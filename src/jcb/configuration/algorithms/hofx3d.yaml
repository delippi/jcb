time window:
  begin: '{{window_begin}}'
  length: '{{window_length}}'
  bound to include: '{{ bound_to_include | default("begin", true) }}'

geometry:
{% filter indent(width=2) %}
{% set geometry_background_file = geometry_background|default('geometry_background', true) ~ '.yaml' %}
{% include geometry_background_file %}
{% endfilter %}

state:
{% filter indent(width=2) %}
{% set background_file = background|default('background', true) ~ '.yaml' %}
{% include background_file %}
{% endfilter %}

observations:
  observers:
{% for observation in observations %}
{% filter indent(width=2) %}
{% include observation + '.yaml' %}
{% endfilter %}
{% endfor %}
